#ifndef _aftiv_ESTIMATING_EQUATIONS_H
#define _aftiv_ESTIMATING_EQUATIONS_H

#include <RcppEigen.h>
#include <vector> 
#include <algorithm> 
#include <iostream>
#include <iterator>
#include <stdlib.h>
#include <numeric>
#include <functional> 

using namespace Rcpp;

RcppExport SEXP AFTScorePre(SEXP, SEXP);

//RcppExport SEXP cumsum_rev_cp(SEXP);

//RcppExport SEXP step_func(SEXP, SEXP, SEXP);

//RcppExport SEXP approx1000(SEXP, SEXP, SEXP);
                               

#endif